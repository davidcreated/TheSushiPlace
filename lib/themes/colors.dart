// lib/themes/colors.dart

import 'package:flutter/material.dart';

// Your primary brand color (darker, suitable for backgrounds or main elements)
const Color primaryColor = Color.fromARGB(255, 138, 60, 55);

// A lighter shade of your primary color (useful for backgrounds or subtle highlights)
const Color lightPrimaryColor = Color.fromARGB(255, 238, 208, 178);

// An accent color for interactive elements, highlights, or call-to-action buttons
// I've chosen a vibrant green to stand out against the darker background.
const Color accentColor = Color.fromARGB(255, 66, 199, 137); // A vibrant green/teal
// You could also try:
// const Color accentColor = Color.fromARGB(255, 255, 170, 51); // A warm orange/amber
// const Color accentColor = Color.fromARGB(255, 100, 181, 246); // A light blue